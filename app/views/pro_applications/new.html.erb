<div class="max-w-xl mx-auto px-4 py-8">
  <h1 class="text-2xl font-bold mb-2">プロ申請</h1>
  <p class="text-sm text-slate-500 mb-6">
    SNSや実績が確認できるリンクを入力してください（例：X / Instagram / YouTube / ダーツ関連ページ）。
  </p>

  <%= form_with model: @user, url: pro_application_path, method: :post, class: "space-y-4" do |f| %>
    <div>
      <%= f.label :pro_sns_url, "SNSリンク（必須）", class: "text-sm font-semibold text-slate-700" %>
      <%= f.url_field :pro_sns_url,
            placeholder: "https://...",
            class: "mt-1 w-full rounded-lg border border-slate-300 px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-sky-500" %>

      <% if @user.errors[:pro_sns_url].present? %>
        <p class="mt-1 text-xs text-red-600"><%= @user.errors[:pro_sns_url].first %></p>
      <% end %>
    </div>

    <div class="flex gap-2">
      <%= f.submit "申請する",
            class: "rounded-lg bg-black px-4 py-2 text-sm font-semibold text-white hover:bg-slate-800" %>
      <%= link_to "キャンセル", mypage_path,
            class: "rounded-lg border border-slate-300 px-4 py-2 text-sm font-semibold text-slate-700 hover:bg-slate-50" %>
    </div>
  <% end %>
</div>