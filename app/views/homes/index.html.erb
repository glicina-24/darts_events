<div class="min-h-screen flex flex-col bg-linear-to-b from-slate-50 to-white">
  <main class="flex-1">
    <section class="relative overflow-hidden">
      <div class="absolute inset-0 bg-linear-to-br from-slate-900 via-slate-800 to-slate-900"></div>
      <div class="relative max-w-6xl mx-auto px-4 py-12 sm:py-16">
        <div class="max-w-2xl mx-auto text-center">
          <div class="inline-flex items-center gap-2 text-sm font-semibold text-white/85">
            <span class="text-lg">🎯</span>
            <span>近くのイベントを見つけよう</span>
          </div>
          <h1 class="mt-6 text-4xl sm:text-6xl font-black tracking-tight">
            <span class="bg-linear-to-r from-white via-sky-100 to-emerald-100 bg-clip-text text-transparent">
              Darts Events
            </span>
          </h1>
          <p class="mt-4 text-lg sm:text-xl text-white/80 leading-relaxed font-medium">
            ダーツイベントを探す・投稿する
          </p>
          <div class="mt-8 flex flex-col sm:flex-row flex-wrap justify-center gap-3">
            <% if user_signed_in? && current_user.shop_owner? %>
              <%= link_to new_event_path,
                    class: "group inline-flex items-center justify-center px-6 py-3.5 rounded-xl bg-linear-to-r from-emerald-400 to-emerald-500 text-white text-base font-bold shadow-lg shadow-emerald-500/30 hover:shadow-xl hover:shadow-emerald-500/40 hover:scale-[1.02] transition-all duration-200" do %>
                <span>✨ イベントを投稿する</span>
                <svg class="ml-2 w-4 h-4 group-hover:translate-x-1 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M9 5l7 7-7 7"/>
                </svg>
              <% end %>
            <% else %>
              <%= link_to (user_signed_in? ? new_shop_path : new_user_session_path),
                    class: "group inline-flex items-center justify-center px-6 py-3.5 rounded-xl bg-linear-to-r from-emerald-400 to-emerald-500 text-white text-base font-bold shadow-lg shadow-emerald-500/30 hover:shadow-xl hover:shadow-emerald-500/40 hover:scale-[1.02] transition-all duration-200" do %>
                <span>🏪 店舗登録して投稿する</span>
                <svg class="ml-2 w-4 h-4 group-hover:translate-x-1 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M9 5l7 7-7 7"/>
                </svg>
              <% end %>
            <% end %>

            <%= link_to events_path,
                  class: "inline-flex items-center justify-center px-6 py-3.5 rounded-xl bg-white/10 backdrop-blur-sm border-2 border-white/30 text-white text-base font-bold hover:bg-white/20 hover:border-white/40 transition-all duration-200" do %>
              <span>🔍 イベントを探す</span>
            <% end %>

            <%= link_to shops_path,
                  class: "inline-flex items-center justify-center px-6 py-3.5 rounded-xl bg-transparent border-2 border-white/30 text-white text-base font-bold hover:bg-white/10 transition-all duration-200" do %>
              <span>🏢 店舗一覧</span>
            <% end %>
          </div>

          <div class="mt-8 flex flex-col sm:flex-row flex-wrap justify-center items-center gap-4 sm:gap-6">
            <div class="flex items-center gap-2 text-sm text-white/90">
              <span class="text-base">📅</span>
              <span class="font-medium">開催日で絞り込み</span>
            </div>
            <div class="flex items-center gap-2 text-sm text-white/90">
              <span class="text-base">🖼️</span>
              <span class="font-medium">画像つきで見やすい</span>
            </div>
            <div class="flex items-center gap-2 text-sm text-white/90">
              <span class="text-base">⭐</span>
              <span class="font-medium">お気に入り対応</span>
            </div>
          </div>
        </div>
      </div>
    </section>

    <section class="max-w-6xl mx-auto px-4 py-12">
      <div class="flex items-end justify-between gap-3 mb-8">
        <div>
          <div class="flex items-center gap-2">
            <span class="text-2xl">🌟</span>
            <h2 class="text-2xl sm:text-3xl font-black text-slate-900">Top Pick!</h2>
          </div>
          <p class="text-sm text-slate-600 mt-2 font-medium">注目イベントをピックアップ</p>
        </div>

        <%= link_to events_path,
              class: "group flex items-center gap-1 text-sm font-bold text-sky-600 hover:text-sky-700 transition-colors" do %>
          <span>もっと見る</span>
          <svg class="w-4 h-4 group-hover:translate-x-1 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M9 5l7 7-7 7"/>
          </svg>
        <% end %>
      </div>

      <% if @top_events.present? %>
        <div class="grid gap-5 sm:grid-cols-2 lg:grid-cols-3">
          <% @top_events.each do |event| %>
            <%= render "events/card", event: event %>
          <% end %>
        </div>
      <% else %>
        <!-- イベントがない場合 -->
        <div class="relative overflow-hidden rounded-2xl bg-linear-to-br from-sky-50 to-emerald-50 border-2 border-dashed border-slate-300 p-8 sm:p-12 text-center">
          <div class="relative z-10">
            <div class="text-6xl mb-4">🎯</div>
            <p class="text-lg font-bold text-slate-700 mb-2">まだイベントがありません</p>
            <p class="text-sm text-slate-600">
              最初のイベントを投稿して、<br class="sm:hidden">コミュニティを盛り上げよう!
            </p>
            <% if user_signed_in? && current_user.shop_owner? %>
              <%= link_to "イベントを投稿する",
                    new_event_path,
                    class: "mt-6 inline-flex items-center gap-2 px-6 py-3 rounded-xl bg-linear-to-r from-sky-500 to-sky-600 text-white font-bold shadow-lg hover:shadow-xl transition-all" %>
            <% end %>
          </div>
          <!-- 背景装飾 -->
          <div class="absolute top-0 right-0 w-32 h-32 bg-sky-200/30 rounded-full blur-2xl"></div>
          <div class="absolute bottom-0 left-0 w-32 h-32 bg-emerald-200/30 rounded-full blur-2xl"></div>
        </div>
      <% end %>

      <!-- CTA Strip -->
      <div class="mt-12 relative overflow-hidden rounded-2xl bg-linear-to-r from-slate-900 via-slate-800 to-slate-900 p-6 sm:p-8 shadow-xl">
        <div class="absolute top-0 right-0 w-64 h-64 bg-gradient-radial from-sky-500/20 to-transparent rounded-full blur-3xl"></div>
        <div class="relative z-10 flex flex-col sm:flex-row items-start sm:items-center justify-between gap-5">
          <div class="flex-1">
            <div class="flex items-center gap-2 mb-2">
              <span class="text-2xl">🔍</span>
              <h3 class="text-lg sm:text-xl font-black text-white">近くのイベントをまとめて探す</h3>
            </div>
            <p class="text-sm text-white/70 font-medium">検索・絞り込みでサクッと見つかる</p>
          </div>
          
          <div class="flex flex-col sm:flex-row gap-3 w-full sm:w-auto">
            <%= link_to "イベント検索へ",
                  events_path,
                  class: "inline-flex items-center justify-center gap-2 px-6 py-3 rounded-xl bg-white text-slate-900 font-bold hover:bg-sky-50 transition-all shadow-lg hover:shadow-xl" %>
            <%= link_to "店舗を見る",
                  shops_path,
                  class: "inline-flex items-center justify-center gap-2 px-6 py-3 rounded-xl border-2 border-white/30 text-white font-bold hover:bg-white/10 transition-all" %>
          </div>
        </div>
      </div>
    </section>
  </main>
</div>
