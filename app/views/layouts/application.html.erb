<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Darts Events" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">

    <meta property="og:type" content="website">
    <meta property="og:site_name" content="Darts Events">
    <meta property="og:title" content="Darts Events">
    <meta property="og:description" content="ダーツイベントを探せる・投稿できるアプリ">
    <meta property="og:url" content="<%= request.original_url %>">
    <meta property="og:image" content="<%= "#{request.base_url}/ogp.png" %>">

    <meta name="twitter:card" content="summary_large_image">
    <meta name="twitter:title" content="Darts Events">
    <meta name="twitter:description" content="ダーツイベントを探せる・投稿できるアプリ">
    <meta name="twitter:image" content="<%= "#{request.base_url}/ogp.png" %>">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="manifest" href="/manifest.json">
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
  </head>

  <body class="min-h-screen flex flex-col">
    <%= render "shared/header" unless @disable_header %>
    <% unless @disable_flash %>
      <%= render "shared/flash" %>
    <% end %>
    <main class="flex-1">
      <%= yield %>
    </main>
    <%= render "shared/footer" unless @disable_header %>
  </body>
</html>
